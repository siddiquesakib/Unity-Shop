import PromoCodeGenerator from '@/components/dashboard/admin/PromoCodeGenerator';

export const metadata = {
  title: 'Promo Codes | Admin Dashboard',
};

export default function PromoPage() {
  return <PromoCodeGenerator />;
}
